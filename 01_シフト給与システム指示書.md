# システム指示書 1:シフト調整・給与計算ツール

## 1. 目的

ナイトクラブのシフト作成〜勤怠〜給与計算を一気通貫で自動化し、店長のシフト管理工数(希望回収・作成・欠員の穴埋め連絡)と、給与計算の工数・ミスをなくす。

## 2. ユーザーと利用チャネル

| ユーザー | チャネル | できること |
|---|---|---|
| スタッフ(アルバイト・社員) | LINE(公式アカウント+LIFF) | 希望シフト提出、確定シフト確認、欠員募集への応募、出退勤打刻、給与明細確認 |
| 店長・マネージャー | Web管理画面 | イベント/必要人員の登録、シフト自動生成・手修正・確定、欠員募集の発行、勤怠修正承認、給与計算実行 |
| 経理・オーナー | Web管理画面 | 給与計算結果の確認・CSV出力、人件費レポート閲覧 |

## 3. 全体フロー

```
[月2回] 店長がイベントカレンダーと必要人員を登録
   ↓
[締切前] LINEでスタッフに希望提出リマインド → スタッフはLIFFで○×提出
   ↓
[締切後] シフト自動生成(充足判定) → 店長が管理画面でドラッグ修正 → 確定
   ↓
[確定時] LINEで各自に確定シフトを配信
   ↓
[不足発生時(生成時 or 当日欠勤時)] 対象者へLINEで欠員募集 → 先着 or 店長承認で確定
   ↓
[毎営業日] LINEで出勤/退勤打刻 → 実働記録
   ↓
[月次] 給与計算(深夜割増・日払い控除・インセンティブ) → 明細をLINE配信、CSVを経理へ
```

## 4. データモデル(Prisma スキーマの骨子)

```prisma
model Staff {
  id            String   @id @default(uuid())
  lineUserId    String?  @unique          // LINE連携後に格納
  name          String
  nameKana      String?
  role          StaffRole                  // RECEPTION / BARTENDER / BARBACK / FLOOR_VIP / CLOAK / SECURITY / MANAGER
  employmentType EmploymentType            // PART_TIME / FULL_TIME / CONTRACT
  hourlyWage    Int                        // 円。社員は月給フィールドを別途
  monthlySalary Int?
  isMinor       Boolean  @default(false)   // 18歳未満は深夜勤務不可のためシフト割当時に弾く
  isActive      Boolean  @default(true)
  skills        Json?                      // {"vip": true, "english": true} 等の柔軟なタグ
  hiredAt       DateTime?                  // 新人判定(3ヶ月未満)に使用
  createdAt     DateTime @default(now())
}

model WageHistory {                        // 時給改定履歴(重要: Staff.hourlyWage は「現在値のキャッシュ」とし、給与計算は必ずこちらを参照)
  id            String   @id @default(uuid())
  staffId       String
  hourlyWage    Int
  effectiveFrom DateTime @db.Date          // この日以降の勤務に適用
  createdAt     DateTime @default(now())
  @@index([staffId, effectiveFrom])
}

model ShiftPeriod {                        // シフト募集期間(半月単位)
  id                 String   @id @default(uuid())
  periodStart        DateTime @db.Date
  periodEnd          DateTime @db.Date
  preferenceDeadline DateTime              // 希望提出締切
  status             String                // COLLECTING / GENERATING / CONFIRMED
}

model BusinessDay {
  id           String   @id @default(uuid())
  businessDate DateTime @unique @db.Date
  eventName    String?                    // 例: "TOKYO TRANCE COLLECTIVE"
  eventType    EventType                  // NORMAL / BIG_EVENT / RENTAL / CLOSED
  expectedCrowd Int?                      // 動員予測(人)
  note         String?
}

model StaffingRequirement {                // 営業日×職種×時間帯の必要人数
  id            String @id @default(uuid())
  businessDayId String
  roleNeeded    StaffRole
  startTime     DateTime                  // 例 20:00
  endTime       DateTime                  // 例 翌5:00
  headcount     Int
}

model ShiftPreference {                    // スタッフの希望
  id            String @id @default(uuid())
  staffId       String
  businessDate  DateTime @db.Date
  availability  Availability               // OK / NG / EARLY_ONLY / LATE_ONLY
  submittedAt   DateTime @default(now())
  @@unique([staffId, businessDate])
}

model ShiftAssignment {                    // 確定シフト
  id            String @id @default(uuid())
  staffId       String
  businessDayId String
  roleAssigned  StaffRole
  plannedStart  DateTime
  plannedEnd    DateTime
  status        ShiftStatus                // DRAFT / CONFIRMED / CANCELLED / ABSENT
}

model OpenShiftRequest {                   // 欠員募集
  id            String @id @default(uuid())
  businessDayId String
  roleNeeded    StaffRole
  start         DateTime
  end           DateTime
  reason        String                     // "生成時不足" / "当日欠勤(〇〇)" / "交代申請"
  status        OpenShiftStatus            // OPEN / FILLED / EXPIRED
  filledByStaffId String?
  version       Int    @default(0)         // 楽観ロック用
}

model ShiftSwapRequest {                    // 事前の交代申請(現場で最頻出のフロー)
  id                String   @id @default(uuid())
  assignmentId      String                 // 手放したいシフト
  requesterStaffId  String
  reason            String?
  status            String                 // PENDING_MANAGER / OPEN_RECRUITING / SWAPPED / REJECTED
  acceptedByStaffId String?
  createdAt         DateTime @default(now())
}

model TimeRecord {                         // 勤怠実績
  id            String @id @default(uuid())
  staffId       String
  businessDate  DateTime @db.Date
  clockIn       DateTime?
  clockOut      DateTime?
  breakMinutes  Int      @default(0)
  editedByAdmin Boolean  @default(false)   // 打刻漏れ修正フラグ
  approved      Boolean  @default(false)
}

model AdvancePayment {                     // 日払い・前払いの台帳
  id           String   @id @default(uuid())
  staffId      String
  businessDate DateTime @db.Date
  amount       Int
  paidBy       String                      // 渡した管理者名
  memo         String?
}

model Incentive {                          // ドリンクバック等の歩合
  id           String   @id @default(uuid())
  staffId      String
  businessDate DateTime @db.Date
  kind         String                      // "bottle_back" / "door_back" など
  amount       Int
}

model PayrollRun {                         // 月次給与計算の実行結果
  id           String   @id @default(uuid())
  periodStart  DateTime @db.Date
  periodEnd    DateTime @db.Date
  status       String                      // DRAFT / FINALIZED
  createdAt    DateTime @default(now())
  items        PayrollItem[]
}

model PayrollItem {
  id              String @id @default(uuid())
  payrollRunId    String
  staffId         String
  totalMinutes    Int
  nightMinutes    Int                      // 22:00-5:00 の分数
  basePay         Int
  nightPremiumPay Int                      // 深夜割増25%分
  incentivePay    Int
  advanceDeduction Int                     // 日払い済み控除
  grossPay        Int
  breakdown       Json                     // 日別内訳(明細表示用)
}
```

## 5. 機能要件

### 5.1 LINE側(スタッフ向け)

LINE公式アカウントのリッチメニューに4ボタンを設置:「希望提出」「シフト確認」「打刻」「給与明細」。それぞれLIFFアプリ(モバイル最適化Webビュー)を開く。

1. **初回登録**: 友だち追加→LIFFで氏名を入力→管理者が管理画面で承認し `Staff` レコードと紐付け(lineUserId を格納)
2. **希望シフト提出**: 対象期間(半月分)のカレンダーが表示され、日ごとに「○ / × / 早番のみ / 遅番のみ」をタップで選択して送信。締切2日前と当日に未提出者へ自動リマインドをプッシュ配信
3. **確定シフト確認**: 自分の確定シフト一覧。確定時・変更時にプッシュ通知
4. **欠員募集への応募**: 募集発生時、条件に合うスタッフ(その職種ができ、その日に確定シフトがなく、希望が×でない者)へFlex Messageを一斉配信。「応募する」ボタンをタップ→設定により「先着順で即確定」または「店長承認後に確定」。確定したら本人へ通知、他の応募者へは締切通知。**先着順の排他制御**: 同時タップに備え、DBトランザクション内で `status=OPEN` の行を条件付きUPDATE(またはversionによる楽観ロック)し、1名だけが確定するようにする。負けた側には「先に決まってしまいました」と返信する
5. **シフト交代申請**: 確定シフトの一覧から「代わってほしい」を選択→理由入力→店長にLINE通知。店長が承認すると、そのシフトが欠員募集として同条件のスタッフに配信され、成立したら割当が付け替わる。**成立するまで元の担当者の勤務義務は消えない**ことをUI文言で明示する
5. **打刻**: 「出勤」「退勤」ボタン。位置情報が取れる場合は記録(取れなくても打刻自体は許可)。打刻忘れはLIFFから修正申請→店長承認
6. **給与明細**: 確定済みの月次明細(総支給・深夜割増・インセンティブ・日払い控除の内訳)を閲覧

### 5.2 Web管理画面(店長・経理向け)

1. **カレンダー管理**: 月間カレンダーに営業日・イベント種別・動員予測を登録。イベント種別ごとの「必要人員テンプレート」(例: BIG_EVENT → バーテンダー4・レセプション2・セキュリティ3…)を設定でき、ワンクリックで必要人員を展開
2. **シフト生成**: 「自動生成」ボタンで下記アルゴリズムを実行し、ドラフトを表示。日×職種のグリッドで、不足セルは赤表示。ドラッグ&ドロップで手修正→「確定」でLINE配信
3. **欠員募集の発行**: 不足セルから「募集をかける」ボタン。当日欠勤の登録(ShiftAssignment を ABSENT に)と同時に募集発行もできる
4. **勤怠管理**: 営業日ごとの打刻一覧、予定との乖離ハイライト、修正申請の承認、月次一括承認
5. **給与計算**: 期間を指定して実行→ドラフト表示→確定。確定でLINE明細配信+CSV出力(会計・給与ソフト取込用の汎用フォーマット:スタッフ名、総労働時間、深夜時間、総支給、控除、差引支給)
6. **レポート**: 営業日別の人件費(実働×時給ベースの概算)、人件費÷動員予測の推移グラフ

### 5.3 シフト自動生成アルゴリズム(フェーズ1は貪欲法でよい)

最適化ライブラリは使わず、以下のルールベースで実装する:

1. 各営業日×職種の必要人数スロットを、充足難易度が高い順(応募可能者が少ない順)に処理
2. 割当候補 = その職種スキルを持ち、希望が○(または該当時間帯OK)で、未割当のスタッフ
3. 優先度スコア: `(a)今期の割当時間が少ない者を優先(公平性) + (b)employmentType=FULL_TIME を優先 + (c)新人(入店3ヶ月未満)は同時間帯にベテランが1人以上いる場合のみ割当可`
4. 制約: 18歳未満(`isMinor`)は22:00以降のスロットに割当禁止 / 同一人物の同日重複割当禁止 / 週の割当合計が設定上限(既定40h)を超えない
5. 埋まらなかったスロットは「不足」として残し、管理画面に赤表示+募集発行を提案

ロジックは純関数として切り出し、ユニットテストを必ず書くこと(公平性・未成年制約・新人制約のテストケースを含める)。

### 5.4 給与計算ロジック(フェーズ1の範囲)

- 対象: 時給制スタッフ(社員の月給は固定額をそのまま計上)
- **打刻の丸め**: 設定値(既定: 丸めなし。5分/15分単位も選択可)。丸め方向は労働者不利にならないよう「出勤は切り捨てず、退勤は切り上げない」既定とし、設定で変更可能にする
- **休憩の扱い**: 打刻に休憩入力がない場合、労基法ベースの自動控除を適用(実労働6時間超で45分、8時間超で60分。設定でON/OFF・分数変更可)。自動控除が適用された明細にはその旨を表示する
- `労働分数 = clockOut - clockIn - breakMinutes(手入力 or 自動控除)`(承認済みTimeRecordのみ)
- **適用時給**: `WageHistory` から勤務日(businessDate)時点で有効な時給を引く。**Staff.hourlyWage を直接使わない**(時給改定時に過去の給与が変わる事故を防ぐ)
- **深夜割増**: 22:00〜翌5:00 に該当する分数を分単位で判定し、その分の時給を1.25倍。日またぎ勤務を正しく処理すること(例: 20:00-翌4:00 勤務 → 通常2h+深夜6h)
- 総支給 = 基本給 + 深夜割増分 + Incentive合計
- 差引支給 = 総支給 − AdvancePayment(日払い)合計
- **確定ロック**: PayrollRun を FINALIZED にすると、対象期間の TimeRecord・AdvancePayment・Incentive は編集不可になる。修正が必要な場合は「再計算(新しいDRAFTを作り直す)」操作のみ許可し、確定済みRunは履歴として残す
- 端数処理・締め日などはすべて管理画面の「設定」で変更可能にする
- **スコープ外(フェーズ2以降)**: 所得税・社会保険・雇用保険の控除計算は行わない。CSVを給与ソフト/社労士へ渡す前提とし、その旨を明細に注記する

## 6. LINE実装の技術詳細

- Webhook エンドポイント: `POST /api/line/webhook`(署名検証必須)
- プッシュ配信はレート制限を考慮し、キュー(DBテーブル+cronでの逐次送信)経由にする
- 欠員募集は Flex Message で「日付/時間/職種/時給/応募ボタン」をカード表示
- LIFF は Next.js 内の `/liff/*` ルートに実装し、LIFF SDK で `liff.getProfile()` から lineUserId を取得して認証する

## 7. 実装フェーズ

- **フェーズ1(MVP)**: スタッフ登録/希望提出/自動生成+手修正/確定配信/打刻/給与計算(上記5.4)/CSV出力
- **フェーズ2**: シフト交代申請フロー、欠員募集の自動マッチング高度化(応募がない場合の条件緩和再配信)、インセンティブ入力画面、人件費レポート
- **フェーズ3**: 動員実績を蓄積し、イベント種別×曜日から必要人員を提案(Claude APIで過去データを要約・提案)

## 8. 受け入れ基準(抜粋)

- [ ] スタッフがLINEだけで「希望提出→確定確認→打刻→明細確認」を完結できる
- [ ] 20:00〜翌4:00の勤務1件で、深夜割増が正しく6時間分計算される(テストで担保)
- [ ] 当日欠勤登録から60秒以内に、条件合致スタッフへ募集がプッシュ配信される
- [ ] 日払い5,000円を登録した月の明細で、差引支給が5,000円減っている
- [ ] 月の途中で時給を1,300円→1,400円に改定した場合、改定日前の勤務は1,300円、以降は1,400円で計算される(テストで担保)
- [ ] 9時間勤務・休憩未入力の打刻に対し、60分の自動休憩控除が適用され明細に表示される
- [ ] 欠員募集に2ユーザーが同時応募しても、確定は必ず1名になる(競合テストで担保)
- [ ] FINALIZED済みの期間の勤怠を編集しようとするとブロックされる
