# ナイトクラブ業務効率化システム群 — 共通前提書

Claude Codeへ:このリポジトリには3つの独立したシステムの指示書が含まれる。各指示書は単体で実装可能だが、以下の共通前提を必ず先に読むこと。

## 背景

- 対象:東京・六本木のナイトクラブ「CROSS ROPPONGI」(バー/ラウンジ+ライブバンドカラオケ+ナイトクラブの複合業態、営業時間はおおむね20:00〜翌5:00)
- 経営課題:人件費等の高コストによる赤字。バックオフィス業務(経理・シフト・労務)の効率化と、新人教育の省力化が急務
- 利用者のITリテラシー:高くない。**スタッフ側のUIは全員が使い慣れているLINEに寄せる**こと。管理者(店長・経理)向けには簡素なWeb管理画面を用意する

## 3システムの構成

| # | システム名 | 指示書 |
|---|---|---|
| 1 | シフト調整・給与計算ツール | `01_シフト給与システム指示書.md` |
| 2 | AI上司システム(教育・Q&Aボット) | `02_AI上司システム指示書.md` |
| 3 | 請求書AI管理システム | `03_請求書管理システム指示書.md` |

## 共通技術スタック(全システム統一)

- **言語/ランタイム**: TypeScript / Node.js 22 LTS
- **フレームワーク**: Next.js (App Router) — 管理画面とAPIを1リポジトリで完結させる
- **DB**: PostgreSQL(Supabase を推奨。Auth・Storage も Supabase を利用)
- **ORM**: Prisma
- **LINE連携**: LINE Messaging API(公式アカウント)+ LIFF(LINE Front-end Framework)。SDK は `@line/bot-sdk` を使用
- **AI**: Anthropic API(`claude-sonnet-4-6` を既定。OCR/画像読解にも同モデルの vision を使用)
- **ホスティング想定**: Vercel(Next.js)+ Supabase。cron は Vercel Cron を使用
- **タイムゾーン**: すべて `Asia/Tokyo`。**深夜営業のため日付境界の扱いに注意**(後述の「営業日」概念を必ず実装)

## LINE公式アカウントの構成(必読)

LINEのWebhookは1チャネルにつき1URLしか設定できないため、アカウント構成を先に確定する。**公式アカウントは2つ作成する**:

| アカウント | 対象 | 載せる機能 |
|---|---|---|
| ①「CROSS スタッフ」 | 全スタッフ | システム1(シフト・打刻・明細)+ システム2(AI上司)。**1つのWebhookエンドポイントで受け、メッセージ内容で振り分ける**(リッチメニュー起点のLIFF操作→システム1、フリーテキスト・画像→システム2、Postback は data のプレフィックス `shift:` / `ai:` で判別) |
| ②「CROSS 管理」 | 店長・経理・オーナーのみ | システム3(請求書取込・支払指示)+ 各システムの運用アラート(後述) |

- モノレポ(Turborepo または npm workspaces)構成とし、`packages/line-router`(Webhook署名検証+イベント振分け)、`packages/shared-db`(Prismaスキーマ共有: Staff等)、`packages/business-date`(営業日ユーティリティ)を共通パッケージ化。アプリ本体は `apps/shift-payroll` `apps/ai-boss` `apps/invoice` の3つ
- DBは**1つのSupabaseプロジェクトを共有**し、テーブルはプレフィックスなしで共存させる(Staffテーブルは3システム共用。二重管理を絶対にしない)
- **LINE配信数の注意**: 無料プランは月200通のプッシュまで。スタッフ30名規模でもシフト通知だけで超過するため、①はコミュニケーションプラン(月5,000通)以上を契約する前提とし、**応答は可能な限り replyToken(無料)を使い、プッシュは必要最小限にする**実装とすること

## 「営業日(business date)」の共通概念(重要)

クラブの1営業日は 20:00〜翌朝10:00 をひとまとまりとして扱う。例:1月10日22時の勤務も1月11日4時の勤務も「1月10日営業日」に属する。

- 実装ルール: `businessDate = (timestamp - 10時間) の日付部分`
- シフト・勤怠・売上・日報など日次で扱うデータはすべて `business_date DATE` カラムを持たせ、カレンダー日付と混同しないこと

## 共通の非機能要件

- 個人情報(氏名・連絡先・給与額)を扱うため、管理画面は必ず認証必須(Supabase Auth、メール+パスワードで可)。ロールは `admin`(オーナー/店長)と `staff` の2種で開始
- LINE Webhook は署名検証(`x-line-signature`)を必ず実装
- 環境変数は `.env.example` を用意し、シークレットをコードに埋め込まない
- 主要ロジック(給与計算・シフト充足判定・請求書重複判定)にはユニットテストを書く(Vitest)
- 文言・UIはすべて日本語

## 共通の信頼性要件(欠落すると現場で事故になるため必須)

- **Webhook冪等性**: LINEはイベントを再送することがある。`webhookEventId` をDBに記録し、処理済みイベントはスキップする(全システム共通実装を `packages/line-router` に置く)
- **失敗キューと再試行**: AI抽出・プッシュ配信・外部API呼び出しは失敗を前提に、ジョブテーブル(status: PENDING/PROCESSING/FAILED/DONE、attempts、lastError)+cron再試行(最大3回、指数バックオフ)で実装。3回失敗したジョブは「管理」アカウントへLINE通知
- **死活監視**: 毎朝10:00に各システムのヘルスチェック(直近24hの取込件数・エラー件数・未処理ジョブ数)を「管理」アカウントに1通で通知。**沈黙=正常ではなく、正常であることを毎日報告する**設計にする(請求書取込が静かに止まる事故の防止)
- **バックアップ**: Supabaseの日次バックアップを有効化する旨をREADMEのセットアップ手順に記載

## 実装の進め方への指示

1. 各指示書の「フェーズ1(MVP)」だけをまず完成させ、動くものを最短で作る。フェーズ2以降は指示書に従い拡張
2. 3システムは上記モノレポ内の別アプリとして実装する。**Staffテーブル・LINEルーター・営業日ユーティリティは共通パッケージを必ず使い、複製しない**
3. 不明点・未確定の業務パラメータ(時給、必要人員数など)はすべて管理画面から設定可能な「設定値」として実装し、ハードコードしない
